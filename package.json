{
  "name": "visualizer-worker",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "preprocess": "ts-node src/index.ts preprocess",
    "build": "tsc",
    "api:dev": "cd api && ts-node src/index.ts",
    "api:build": "cd api && tsc",
    "api:start": "cd api && node dist/index.js"
  },
  "dependencies": {
    "audio-decode": "^0.2.1",
    "fft-js": "^0.0.12"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  }
}
